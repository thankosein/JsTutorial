Got it — let’s make your 2.4 Example ready for hands-on testing and follow best practices for DOM element selection and storage.

---

## Best Practice Version for Testing

 HTML (test.html)

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>DOM Selection Example</title>
</head>
<body>
  <h1 id="page-title">Fruit List</h1>

  <ul id="fruits">
    <li class="item">Apple</li>
    <li class="item">Banana</li>
    <li class="item">Cherry</li>
  </ul>

  <script src="script.js"></script>
</body>
</html>
```

---

 JavaScript (script.js)

```javascript
// ✅ Best Practice: Cache selectors in constants
const pageTitle = document.getElementById("page-title");
const fruitsList = document.querySelector("#fruits");
const fruitItems = document.querySelectorAll(".item");

// ✅ Best Practice: Avoid innerHTML unless necessary (security & performance)
console.log("Page Title:", pageTitle.textContent);
console.log("Fruits List Tag:", fruitsList.tagName);

// ✅ Best Practice: Loop using forEach (works on NodeList)
fruitItems.forEach((fruit, index) => {
  console.log(`Fruit ${index + 1}:`, fruit.textContent);
});

// Example: Changing text
pageTitle.textContent = "Updated Fruit List";

// Example: Adding a new fruit
const newFruit = document.createElement("li");
newFruit.classList.add("item");
newFruit.textContent = "Mango";
fruitsList.appendChild(newFruit);

// Recheck the updated list
console.log("Updated Fruits:");
document.querySelectorAll(".item").forEach(fruit => {
  console.log(fruit.textContent);
});
```

---

 Best Practices Applied

1. Use `const` for DOM elements
   Elements don’t change references — constants make intent clear.

2. Prefer `querySelector` / `querySelectorAll`
   Flexible and supports complex CSS selectors.

3. Cache selectors
   Don’t repeatedly query the DOM for the same element — it’s slow.

4. Use `textContent` instead of `innerHTML`

   * Safer (avoids XSS injection risks)
   * Faster (no HTML parsing unless needed)

5. Use `forEach` on NodeLists
   More readable and modern compared to traditional loops.

6. Make changes in batches
   If adding multiple elements, consider `DocumentFragment` to reduce reflows.

---

If you run `test.html` in your browser and open Developer Tools → Console, you’ll see the output and the changes live.

---

If you like, I can extend this into an interactive playground where you can type a fruit name in an `<input>` box and dynamically add it to the list — that would be the next logical step to practice DOM selection + manipulation.

Do you want me to make that interactive version next?
